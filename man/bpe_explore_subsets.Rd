% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bpe.R
\name{bpe_explore_subsets}
\alias{bpe_explore_subsets}
\title{Explore Candidate BPE Subsets}
\usage{
bpe_explore_subsets(
  data,
  spec,
  rules = NULL,
  seed = 1,
  min_n_S = NULL,
  max_F_S = NULL,
  min_varZ_S = NULL,
  return_all = TRUE
)
}
\arguments{
\item{data}{Data frame used for estimation.}

\item{spec}{Either an IV formula (\code{y ~ X | Z}) or a list with at least
\code{formula}, and optional \code{fe}, \code{fe_engine}, and \code{z_names}.}

\item{rules}{Candidate subset definitions. Each candidate may be a one-sided
formula evaluated in \code{data}, a \verb{function(data)} returning a logical vector,
a character string naming a logical column in \code{data}, a logical vector of
length \code{nrow(data)}, or a named list with components \code{name} and \code{subset}.}

\item{seed}{Integer seed for deterministic rule evaluation.}

\item{min_n_S}{Optional exploratory screen for subset size.}

\item{max_F_S}{Deprecated exploratory screen for the first-stage F-statistic.
The first-stage F-statistic is reported as a diagnostic only.}

\item{min_varZ_S}{Optional exploratory screen for within-subset residualized
instrument variance.}

\item{return_all}{If \code{TRUE} (default), returns all candidates and diagnostics.
If \code{FALSE}, returns the first candidate in the user-supplied order. No
automatic subset search or confirmatory selection is performed.}
}
\value{
If \code{return_all = TRUE}, a data frame with one row per rule and a list
column \code{subset}. If \code{return_all = FALSE}, a list with the first supplied
\code{subset}, \code{rule}, and \code{diagnostics}.
}
\description{
Explores user-supplied candidate subset definitions and reports diagnostics
for transparent exploratory work. This function is not valid confirmatory BPE
by itself. Confirmatory BPE requires a pre-specified \code{bpe_design()} object
supplied to \code{spliv()} or \code{bpe_validate_design()}.
}
\details{
Warning: this function is exploratory. It should not be used for
confirmatory BPE inference. Confirmatory BPE requires a pre-specified
\code{bpe_design()} object with a substantive rationale.
}
\examples{
d <- data.frame(y = rnorm(40), x = rnorm(40), z = rnorm(40),
  inactive = rep(c(TRUE, FALSE), each = 20))
suppressWarnings(bpe_explore_subsets(
  d, y ~ x | z, rules = list(inactive = ~ inactive)))

}
