% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{spliv_tipping_point}
\alias{spliv_tipping_point}
\title{Extract Tipping-Point Delta from a Sensitivity Path}
\usage{
spliv_tipping_point(x)
}
\arguments{
\item{x}{A \code{spliv_sensitivity_path} object.}
}
\value{
Named numeric vector of tipping-point values.
}
\description{
Returns the tipping-point attribute stored on a
\code{spliv_sensitivity_path()} object. For each term, the tipping point is the
smallest \code{delta} on the supplied grid whose interval includes zero.
}
\examples{
set.seed(6)
d <- data.frame(y = rnorm(80), x = rnorm(80), z = rnorm(80))
p <- spliv_sensitivity_path(y ~ x | z, d, method = "uci", delta_grid = c(0, 0.1))
spliv_tipping_point(p)
}
