% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bpe_design.R
\name{bpe_design}
\alias{bpe_design}
\title{Create a Confirmatory BPE Design Object}
\usage{
bpe_design(
  name,
  subset,
  rationale,
  variables_used = NULL,
  subset_type = NULL,
  pre_specified = TRUE,
  transportability_rationale = NULL,
  notes = NULL
)
}
\arguments{
\item{name}{Short design name.}

\item{subset}{Subset specification. Accepts a one-sided formula, a
\verb{function(data)}, a logical vector of length \code{nrow(data)}, or a character
string naming a logical column in \code{data}.}

\item{rationale}{Non-empty substantive justification for the subset.}

\item{variables_used}{Optional character vector describing the design
variables used to define the subset. This is required for function-based
subsets unless the package can safely infer the variables.}

\item{subset_type}{Optional short label such as \code{"theory_defined"} or
\code{"design_based"}.}

\item{pre_specified}{Logical indicator for confirmatory use. Confirmatory BPE
requires \code{TRUE}.}

\item{transportability_rationale}{Optional description of why the direct
effect learned in the subset may be informative for the target sample.}

\item{notes}{Optional free-form notes.}
}
\value{
An object of class \code{"spliv_bpe_design"}.
}
\description{
Creates a researcher-specified design object for confirmatory BPE. The subset
must be justified outside the outcome analysis and should represent a
theoretically motivated instrument-inactive subset.
}
\examples{
design <- bpe_design(
  "Inactive subset", ~ inactive,
  rationale = "The treatment channel is absent in this subset.",
  variables_used = "inactive", pre_specified = TRUE
)
bpe_eval_subset(design, data.frame(inactive = c(TRUE, FALSE)))
}
