% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bpe.R
\name{estimate_gamma_zero_first_stage}
\alias{estimate_gamma_zero_first_stage}
\title{Legacy Exploratory BPE Prior Helper}
\usage{
estimate_gamma_zero_first_stage(
  data,
  y_name,
  z_names,
  controls = character(0),
  subset,
  fe = NULL
)
}
\arguments{
\item{data}{Data frame.}

\item{y_name}{Outcome variable name.}

\item{z_names}{Instrument names whose direct effects are estimated.}

\item{controls}{Optional character vector of additional controls.}

\item{subset}{Legacy subset specification. Accepts a \code{bpe_design()} object, a
one-sided formula, a \verb{function(data)}, a logical vector, or a character
string naming a logical column in \code{data}.}

\item{fe}{Optional one-sided FE formula. If supplied, uses \code{fixest::feols}.}
}
\value{
List with \code{mu_hat}, \code{omega_hat}, and diagnostics.
}
\description{
\code{estimate_gamma_zero_first_stage()} is retained as a legacy exploratory
helper. It is not sufficient for confirmatory BPE inference. Confirmatory
BPE requires \code{bpe_design()}, \code{bpe_validate_design()}, and
\code{spliv(method = "bpe", ...)}.
}
\examples{
d <- data.frame(y = rnorm(40), z = rnorm(40), inactive = rep(c(TRUE, FALSE), each = 20))
suppressWarnings(estimate_gamma_zero_first_stage(
  d, y_name = "y", z_names = "z", subset = ~ inactive))
}
