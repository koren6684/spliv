% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spliv.R
\name{spliv_pattern}
\alias{spliv_pattern}
\title{Create a Patterned Exclusion-Violation Object}
\usage{
spliv_pattern(
  name,
  pattern,
  rationale,
  variables_used = NULL,
  pattern_type = NULL,
  normalize = c("max_abs", "sd", "none"),
  center = FALSE,
  pre_specified = TRUE,
  notes = NULL
)
}
\arguments{
\item{name}{Short pattern name.}

\item{pattern}{Pattern specification. Accepts a one-sided formula, a
\verb{function(data)}, a numeric vector of length \code{nrow(data)}, a logical vector
of length \code{nrow(data)}, or a character string naming a numeric or logical
column in \code{data}.}

\item{rationale}{Substantive justification for the proposed pattern.}

\item{variables_used}{Optional character vector describing the variables used
to define the pattern.}

\item{pattern_type}{Optional short label such as \code{"theory_defined"} or
\code{"design_based"}.}

\item{normalize}{One of \code{"max_abs"} (default), \code{"sd"}, or \code{"none"}.}

\item{center}{Logical; if \code{TRUE}, subtracts the estimation-sample mean before
normalization.}

\item{pre_specified}{Logical indicator for whether the pattern was chosen
before examining outcome results.}

\item{notes}{Optional free-form notes.}
}
\value{
An object of class \code{"spliv_pattern"}.
}
\description{
Creates a researcher-specified direct-effect pattern for patterned
sensitivity analysis. The pattern determines where direct effects of the
instrument are expected to be larger or smaller in the estimation sample.
}
\examples{
p <- spliv_pattern(
  name = "Exposure", pattern = ~ exposure,
  rationale = "The alternative channel follows exposure.",
  variables_used = "exposure", pattern_type = "theory_defined"
)
p$name
}
