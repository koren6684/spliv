% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{spliv_sensitivity_path}
\alias{spliv_sensitivity_path}
\title{Sensitivity Path over Delta Grid}
\usage{
spliv_sensitivity_path(
  formula,
  data,
  method = c("uci", "ltz"),
  delta_grid = seq(0, 0.2, by = 0.01),
  violation_pattern = NULL,
  stop_on_error = TRUE,
  ...
)
}
\arguments{
\item{formula}{IV formula \code{y ~ X | Z}.}

\item{data}{Data frame.}

\item{method}{One of \code{"uci"} or \code{"ltz"}. \code{spliv_sensitivity_path()}
intentionally excludes confirmatory BPE.}

\item{delta_grid}{Non-negative sensitivity grid. For UCI, each value \code{d}
implies theta bounds \verb{[-d, +d]}.}

\item{violation_pattern}{Optional \code{spliv_pattern()} object. If omitted, the
path uses the package's backward-compatible uniform direct-effect pattern.}

\item{stop_on_error}{Logical; if \code{TRUE} (default), stop on the first failed
fit. If \code{FALSE}, record \code{NA} rows and store the error message in the
returned \code{error} column.}

\item{...}{Additional named arguments passed through to \code{spliv()}, such as
\code{fe}, \code{vcov}, \code{cluster}, \code{scale_instrument}, or \code{grid = list(level = 0.95)}.}
}
\value{
A data frame with class \code{c("spliv_sensitivity_path", "data.frame")}.
The returned object includes path columns such as \code{delta}, \code{method},
\code{estimate}, \code{conf_low}, \code{conf_high}, \code{contains_zero}, and pattern metadata,
plus attributes containing the original call, the supplied grid, and a
tipping-point summary.
}
\description{
Runs \code{spliv()} repeatedly over a user-supplied \code{delta_grid} and returns a
tidy sensitivity-path object. This is the recommended workflow for patterned
or uniform sensitivity analysis: users should usually report how intervals
change over a range of \code{delta} values rather than selecting one arbitrary
sensitivity level.
}
\examples{
set.seed(5)
d <- data.frame(y = rnorm(80), x = rnorm(80), z = rnorm(80))
p <- spliv_sensitivity_path(y ~ x | z, d, method = "uci",
  delta_grid = c(0, 0.1), vcov = "hc1")
head(p)
}
